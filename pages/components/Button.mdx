---
name: Button
---

import { Playground, TableOfContents } from 'dokz';
import { HiDotsVertical } from 'react-icons/hi';
import { Box, Stack, Inline, Button, ThemeProvider, Icon } from '../../lib';

<TableOfContents />

## Variant

<Playground>
  <Stack space="xl" css={{ py: '$8', bc: '$gray050' }}>
    <Inline space="sm" css={{ px: '$10' }}>
      <Button>Primary</Button>
      <Button disabled>Primary (disabled)</Button>
    </Inline>
    <Inline space="sm" css={{ px: '$10' }}>
      <Button variant="outline">Outline</Button>
      <Button variant="outline" disabled>
        Outline (disabled)
      </Button>
    </Inline>
    <Inline space="sm" css={{ px: '$10' }}>
      <Button variant="secondary">Secondary</Button>
      <Button variant="secondary" disabled>
        Secondary (disabled)
      </Button>
    </Inline>
    <Inline space="sm" css={{ px: '$10' }}>
      <Button variant="text">Text</Button>
      <Button variant="text" disabled>
        Text (disabled)
      </Button>
    </Inline>
    <Inline space="sm" css={{ px: '$10' }}>
      <Button variant="link" as="a" href="#">
        Link
      </Button>
      <Button variant="link" as="a" href="#" disabled>
        Link (disabled)
      </Button>
    </Inline>
  </Stack>
</Playground>

## Theming

Use `ThemeProvider` component to change the primary color of the button
(or any other component that uses primary colors)

<Playground>
  <ThemeProvider theme="black">
    <Stack space="xl" css={{ py: '$8', bc: '$gray050' }}>
      <Inline space="sm" css={{ px: '$10' }}>
        <Button>Primary</Button>
        <Button disabled>Primary (disabled)</Button>
      </Inline>
      <Inline space="sm" css={{ px: '$10' }}>
        <Button variant="outline">Outline</Button>
        <Button variant="outline" disabled>
          Outline (disabled)
        </Button>
      </Inline>
      <Inline space="sm" css={{ px: '$10' }}>
        <Button variant="secondary">Secondary</Button>
        <Button variant="secondary" disabled>
          Secondary (disabled)
        </Button>
      </Inline>
      <Inline space="sm" css={{ px: '$10' }}>
        <Button variant="text">Text</Button>
        <Button variant="text" disabled>
          Text (disabled)
        </Button>
      </Inline>
      <Inline space="sm" css={{ px: '$10' }}>
        <Button variant="link" as="a" href="#">
          Link
        </Button>
        <Button variant="link" as="a" href="#" disabled>
          Link (disabled)
        </Button>
      </Inline>
    </Stack>
  </ThemeProvider>
</Playground>

## With elements

<Playground>
  <Stack space="xl" css={{ p: '$8', bc: '$gray050' }}>
    <Button>
      <Icon as={HiDotsVertical} /> Primary
    </Button>
  </Stack>
</Playground>
