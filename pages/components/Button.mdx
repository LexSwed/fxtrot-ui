---
name: Button
---

import { Playground } from 'dokz';
import { Box, Button } from '../../build';

# Button

<Playground>
  <Box css={{ p: '$8', display: 'flex', gap: '$3' }}>
    <Button disabled>Make Wow</Button>
    <Button variant="outline">Make Wow</Button>
  </Box>
</Playground>
