---
name: Button
---

import { Playground } from 'dokz';
import { Box, Stack, Button, ThemeProvider } from '../../build';

# Button

<Playground>
  <Stack space="xl" css={{ py: '$8', bc: '$gray050' }}>
    <Box css={{ px: '$8', display: 'flex', gap: '$3' }}>
      <Button>Primary</Button>
      <Button disabled>Primary (disabled)</Button>
    </Box>
    <Box css={{ px: '$8', display: 'flex', gap: '$3' }}>
      <Button variant="secondary">Secondary</Button>
      <Button variant="secondary" disabled>
        Secondary (disabled)
      </Button>
    </Box>
    <Box css={{ px: '$8', display: 'flex', gap: '$3' }}>
      <Button variant="text">Text</Button>
      <Button variant="text" disabled>
        Text (disabled)
      </Button>
    </Box>
    <Box css={{ px: '$8', display: 'flex', gap: '$3' }}>
      <Button variant="link" as="a" href="#">
        Link
      </Button>
      <Button variant="link" as="a" href="#" disabled>
        Link (disabled)
      </Button>
    </Box>
  </Stack>
</Playground>

## Shades

Use `ThemeProvider` component to change the primary color of the button
(or any other component that uses primary colors)

<Playground>
  <ThemeProvider theme="black">
    <Stack space="xl" css={{ py: '$8', bc: '$gray050' }}>
      <Box css={{ px: '$8', display: 'flex', gap: '$3' }}>
        <Button>Primary</Button>
        <Button disabled>Primary (disabled)</Button>
      </Box>
      <Box css={{ px: '$8', display: 'flex', gap: '$3' }}>
        <Button variant="secondary">Secondary</Button>
        <Button variant="secondary" disabled>
          Secondary (disabled)
        </Button>
      </Box>
      <Box css={{ px: '$8', display: 'flex', gap: '$3' }}>
        <Button variant="text">Text</Button>
        <Button variant="text" disabled>
          Text (disabled)
        </Button>
      </Box>
      <Box css={{ px: '$8', display: 'flex', gap: '$3' }}>
        <Button variant="link" as="a" href="#">
          Link
        </Button>
        <Button variant="link" as="a" href="#" disabled>
          Link (disabled)
        </Button>
      </Box>
    </Stack>
  </ThemeProvider>
</Playground>
