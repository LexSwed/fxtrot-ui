---
name: CheckboxGroup
---

import { Playground } from 'dokz';
import { Box, CheckboxGroup, Checkbox } from '@fxtrot/ui';

# CheckboxGroup

<Playground>
  {() => {
    const [colors, setColors] = React.useState(['red']);
    return (
      <Box p="$4">
        <CheckboxGroup label="Select favorite colors" value={colors} onChange={setColors}>
          <Checkbox label="Red" value="red" />
          <Checkbox label="Blue" value="blue" />
          <Checkbox label="Green" value="green" />
          <Checkbox label="Black" value="black" hint="Life is always black" defaultChecked disabled />
        </CheckboxGroup>
      </Box>
    );
  }}
</Playground>
