---
name: Dialog
---

import { Playground } from 'dokz';
import { Dialog, Button, TextField, Flex, Box, Switch } from '@fxtrot/ui';

## Dialog

<Playground>
  <Box p="$8">
    <Dialog>
      <Button>New workspace</Button>
      {(close) => (
        <Dialog.Modal>
          <Dialog.Title level={3}>Create new workspace</Dialog.Title>
          <Box
            pt="$4"
            as="form"
            onSubmit={(e) => {
              e.preventDefault();
              window.alert(e.currentTarget['workspace-name'].value);
            }}
          >
            <TextField placeholder="Carefive" name="workspace-name" label="Workspace name" />
            <Box pb="$10" />
            <Flex flow="row-reverse" gap="sm">
              <Button type="submit" variant="primary">
                Create
              </Button>
              <Button onClick={close} variant="flat">
                Cancel
              </Button>
            </Flex>
          </Box>
        </Dialog.Modal>
      )}
    </Dialog>
  </Box>
</Playground>

## Default Open

<Playground>
  {() => {
    const [defaultOpen, setDefaultOpen] = React.useState(false);
    return (
      <Box p="$8">
        <Flex flow="row" cross="center" main="start" gap="sm">
          <Box>
            <Switch label="Default open" value={defaultOpen} onChange={setDefaultOpen} />
          </Box>
          {defaultOpen && (
            <Dialog defaultOpen={defaultOpen}>
              <Button>New workspace</Button>
              {(close) => (
                <Dialog.Modal>
                  <Dialog.Title level={3}>Create new workspace</Dialog.Title>
                  <Box
                    pt="$4"
                    as="form"
                    onSubmit={(e) => {
                      e.preventDefault();
                      window.alert(e.currentTarget['workspace-name'].value);
                    }}
                  >
                    <TextField placeholder="Carefive" name="workspace-name" label="Workspace name" />
                    <Box pb="$10" />
                    <Flex flow="row-reverse" gap="sm">
                      <Button type="submit" variant="primary">
                        Create
                      </Button>
                      <Button onClick={close} variant="flat">
                        Cancel
                      </Button>
                    </Flex>
                  </Box>
                </Dialog.Modal>
              )}
            </Dialog>
          )}
        </Flex>
      </Box>
    );
  }}
</Playground>
