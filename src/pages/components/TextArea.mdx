---
name: TextArea
---

import { Playground } from 'dokz';
import { Box, Flex, TextArea } from '@fxtrot/ui';

## TextArea

`TextField`-like element but `textarea` and with automatic size adjustment.

<Playground>
  <Flex gap="md" css={{ p: '$6', bc: '$gray50' }}>
    <TextArea label="Enter your name" placeholder="John Doe" />
    <TextArea label="Enter your name" placeholder="John Doe" disabled />
    <TextArea label="Enter your name" placeholder="John Doe" readOnly />
  </Flex>
</Playground>

## Variants

<Playground>
  <Flex gap="lg" css={{ p: '$6', bc: '$gray50' }}>
    <Flex flow="column" gap="md">
      <TextArea label="Boxed" hint="Default variant" placeholder="Default boxed variant" />
      <TextArea variant="underlined" hint="Pretty variant" label="Underlined" placeholder="Pretty underlined" />
      <TextArea variant="transparent" hint="Not using labels" placeholder="Transparent variant" />
      <TextArea variant="flat" label="Flat variant" hint="Press Esc to cancel" placeholder="Flat variant" />
    </Flex>
    <Flex flow="column" gap="md">
      <TextArea
        label="Boxed"
        secondaryLabel="(disabled)"
        hint="Default variant"
        placeholder="Default boxed variant"
        disabled
      />
      <TextArea
        variant="underlined"
        hint="Pretty variant"
        secondaryLabel="(disabled)"
        label="Underlined"
        placeholder="Pretty underlined"
        disabled
      />
      <TextArea variant="transparent" hint="Not using labels" placeholder="Transparent variant" disabled />
      <TextArea disabled variant="flat" label="Flat variant" hint="Press Esc to cancel" placeholder="Flat variant" />
    </Flex>
  </Flex>
</Playground>

## Size

<Playground>
  <Flex gap="lg" css={{ p: '$6', bc: '$gray50' }}>
    <Flex flow="column" gap="md">
      <TextArea size="sm" label="Boxed" hint="Default variant" placeholder="Small size" />
      <TextArea size="sm" variant="underlined" hint="Pretty variant" label="Underlined" placeholder="Small size" />
      <TextArea size="sm" variant="transparent" hint="Not using labels" placeholder="Small size" />
    </Flex>
    <Flex flow="column" gap="md">
      <TextArea size="md" label="Boxed" hint="Default variant" placeholder="Default size" />
      <TextArea size="md" variant="underlined" hint="Pretty variant" label="Underlined" placeholder="Default size" />
      <TextArea size="md" variant="transparent" hint="Not using labels" placeholder="Default size" />
    </Flex>
    <Flex flow="column" gap="md">
      <TextArea size="lg" label="Boxed" hint="Default variant" placeholder="Large size" />
      <TextArea size="lg" variant="underlined" hint="Pretty variant" label="Underlined" placeholder="Large size" />
      <TextArea size="lg" variant="transparent" hint="Not using labels" placeholder="Large size" />
    </Flex>
    <Flex flow="column" gap="md">
      <TextArea size="xl" label="Boxed" hint="Default variant" placeholder="Editable heading size" />
      <TextArea
        size="xl"
        variant="underlined"
        hint="Pretty variant"
        label="Underlined"
        placeholder="Editable heading size"
        as="textarea"
      />
      <TextArea size="xl" variant="transparent" hint="Not using labels" placeholder="Editable heading size" />
    </Flex>
  </Flex>
</Playground>

## Validity

<Playground>
  {() => {
    const [value, setValue] = React.useState('me@email.com');
    const isValid = React.useMemo(() => /^\w+([\.-]?\w+)*@\w+([\.-]?\w+)*(\.\w{2,3})+$/.test(value), [value]);
    return (
      <Box css={{ p: '$6', bc: '$gray50' }}>
        <Flex gap="md" cross="end">
          <TextArea
            cross="stretch"
            validity={isValid ? 'valid' : 'invalid'}
            label="Email"
            hint={!isValid ? 'Check that your email is typed correctly' : ' '}
            placeholder="john.doe@mail.com"
            value={value}
            onChange={setValue}
          />
          <TextArea
            cross="stretch"
            validity={isValid ? 'valid' : 'invalid'}
            label="Email"
            hint={!isValid ? 'Check that your email is typed correctly' : ' '}
            placeholder="john.doe@mail.com"
            value={value}
            onChange={setValue}
            variant="underlined"
          />
          <TextArea
            cross="stretch"
            validity={isValid ? 'valid' : 'invalid'}
            hint={!isValid ? 'Check that your email is typed correctly' : ' '}
            placeholder="john.doe@mail.com"
            value={value}
            onChange={setValue}
            variant="transparent"
          />
        </Flex>
      </Box>
    );
  }}
</Playground>

## Label alignment

Since input is wrapped in `Flex` component, you can easily pass any props `Flex` supports,
for example to specify flow direction:

<Playground>
  <Box css={{ p: '$6', bc: '$gray50' }}>
    <Box css={{ 'width': '420px', '@tablet': { width: '300px' } }}>
      <TextArea
        label="Enter your name"
        flow={{ '@desktop': 'row', '@tablet': 'column' }}
        gap={{ '@desktop': '6', '@initial': '2' }}
        placeholder="John Doe"
        hint="Real name will help your friends to find you!"
      />
    </Box>
  </Box>
</Playground>
