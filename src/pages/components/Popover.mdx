---
name: Popover
---

import { Playground } from 'dokz';
import { Box, Flex, Text, TextField, Button, Popover } from '@fxtrot/ui';

# Popover

<Playground>
  {() => {
    const ref = React.useRef({});
    return (
      <Popover ref={ref}>
        <Button variant="primary">Create</Button>
        <Popover.Content css={{ width: '300px' }} offset={12} placement="bottom-end">
          <Flex gap="4" flow="column">
            <TextField label="Full name" placeholder="Ahsoka Tano" />
            <Flex gap="2" main="end">
              <Button
                onClick={() => {
                  ref.current.close();
                }}
              >
                Cancel
              </Button>
              <Button variant="primary" css={{ alignSelf: 'flex-end' }}>
                Submit
              </Button>
            </Flex>
          </Flex>
        </Popover.Content>
      </Popover>
    );
  }}
</Playground>
