---
name: Button
---

import { Playground } from 'dokz';
import { HiDotsVertical } from 'react-icons/hi';
import { Box, Stack, Button, ThemeProvider, Icon } from '@fxtrot/ui';

## Variant

<Playground>
  <Stack space="xl" css={{ py: '$8', bc: '$gray050' }}>
    <Stack flow="row" space="sm" css={{ px: '$10' }}>
      <Button>Primary</Button>
      <Button disabled>Primary (disabled)</Button>
    </Stack>
    <Stack flow="row" space="sm" css={{ px: '$10' }}>
      <Button variant="outline">Outline</Button>
      <Button variant="outline" disabled>
        Outline (disabled)
      </Button>
    </Stack>
    <Stack flow="row" space="sm" css={{ px: '$10' }}>
      <Button variant="secondary">Secondary</Button>
      <Button variant="secondary" disabled>
        Secondary (disabled)
      </Button>
    </Stack>
    <Stack flow="row" space="sm" css={{ px: '$10' }}>
      <Button variant="flat">Flat</Button>
      <Button variant="flat" disabled>
        Flat (disabled)
      </Button>
    </Stack>
    <Stack flow="row" space="sm" css={{ px: '$10' }}>
      <Button variant="link" as="a" href="#">
        Link
      </Button>
      <Button variant="link" as="a" href="#" disabled>
        Link (disabled)
      </Button>
    </Stack>
  </Stack>
</Playground>

## Theming

Use `ThemeProvider` component to change the primary color of the button.

<Playground>
  <ThemeProvider theme="black">
    <Stack space="xl" css={{ py: '$8', bc: '$surfaceStill' }}>
      <Stack flow="row" space="sm" css={{ px: '$10' }}>
        <Button>Primary</Button>
        <Button disabled>Primary (disabled)</Button>
      </Stack>
      <Stack flow="row" space="sm" css={{ px: '$10' }}>
        <Button variant="secondary">Secondary</Button>
        <Button variant="secondary" disabled>
          Secondary (disabled)
        </Button>
      </Stack>
      <Stack flow="row" space="sm" css={{ px: '$10' }}>
        <Button variant="flat">Flat</Button>
        <Button variant="flat" disabled>
          Flat (disabled)
        </Button>
      </Stack>
      <Stack flow="row" space="sm" css={{ px: '$10' }}>
        <Button variant="link" as="a" href="#">
          Link
        </Button>
        <Button variant="link" as="a" href="#" disabled>
          Link (disabled)
        </Button>
      </Stack>
    </Stack>
  </ThemeProvider>
</Playground>

## With elements

<Playground>
  <Stack space="xl" css={{ p: '$8', bc: '$gray050' }}>
    <Button>
      <Icon as={HiDotsVertical} />
      <span>Wrapped</span>
    </Button>
    <Button>
      <Icon as={HiDotsVertical} />
      Not wrapped
    </Button>
    <Button variant="outline">
      <Icon as={HiDotsVertical} variant="secondary" />
    </Button>
  </Stack>
</Playground>
