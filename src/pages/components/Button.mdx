---
name: Button
---

import { Playground } from 'dokz';
import { DotsVerticalIcon } from '@heroicons/react/solid';
import { Box, Flex, Grid, Button, ThemeProvider, Icon } from '@fxtrot/ui';

## Variant

<Playground>
  <Grid gap="lg" cross="center" main="center" columns="repeat(2, auto)" css={{ p: '$4', bc: '$gray50' }}>
    <Button variant="primary">Primary</Button>
    <Button disabled variant="primary">
      Primary (disabled)
    </Button>
    <Button variant="outline">Outline</Button>
    <Button variant="outline" disabled>
      Outline (disabled)
    </Button>
    <Button variant="secondary">Secondary (default)</Button>
    <Button variant="secondary" disabled>
      Secondary (disabled)
    </Button>
    <Button variant="flat">Flat</Button>
    <Button variant="flat" disabled>
      Flat (disabled)
    </Button>
    <Button variant="link" as="a" href="#">
      Link
    </Button>
    <Button variant="link" as="a" href="#" disabled>
      Link (disabled)
    </Button>
  </Grid>
</Playground>

## Size

<Playground>
  <Flex flow="column" gap="xl" css={{ py: '$8', bc: '$gray50' }}>
    <Flex gap="sm" css={{ px: '$10' }}>
      <Button size="xs" variant="primary">
        Primary
      </Button>
      <Button size="xs" variant="outline">
        Outline
      </Button>
      <Button size="xs" variant="secondary">
        Secondary
      </Button>
      <Button size="xs" variant="flat">
        Flat
      </Button>
      <Button size="xs" variant="link" as="a" href="#">
        Link
      </Button>
    </Flex>
    <Flex gap="sm" css={{ px: '$10' }}>
      <Button size="sm" variant="primary">
        Primary
      </Button>
      <Button size="sm" variant="outline">
        Outline
      </Button>
      <Button size="sm" variant="secondary">
        Secondary
      </Button>
      <Button size="sm" variant="flat">
        Flat
      </Button>
      <Button size="sm" variant="link" as="a" href="#">
        Link
      </Button>
    </Flex>
    <Flex gap="sm" css={{ px: '$10' }}>
      <Button size="md" variant="primary">
        Primary
      </Button>
      <Button size="md" variant="outline">
        Outline
      </Button>
      <Button size="md" variant="secondary">
        Secondary
      </Button>
      <Button size="md" variant="flat">
        Flat
      </Button>
      <Button size="md" variant="link" as="a" href="#">
        Link
      </Button>
    </Flex>
    <Flex gap="sm" css={{ px: '$10' }}>
      <Button size="lg" variant="primary">
        Primary
      </Button>
      <Button size="lg" variant="outline">
        Outline
      </Button>
      <Button size="lg" variant="secondary">
        Secondary
      </Button>
      <Button size="lg" variant="flat">
        Flat
      </Button>
      <Button size="lg" variant="link" as="a" href="#">
        Link
      </Button>
    </Flex>
  </Flex>
</Playground>

## Theming

Use `ThemeProvider` component to change the primary color of the button.

<Playground>
  <ThemeProvider theme="purple">
    <Flex flow="column" gap="xl" css={{ py: '$8', bc: '$surfaceStill' }}>
      <Flex gap="sm" css={{ px: '$10' }}>
        <Button variant="primary">Primary</Button>
        <Button disabled variant="primary">
          Primary (disabled)
        </Button>
      </Flex>
      <Flex gap="sm" css={{ px: '$10' }}>
        <Button variant="secondary">Secondary</Button>
        <Button variant="secondary" disabled>
          Secondary (disabled)
        </Button>
      </Flex>
      <Flex gap="sm" css={{ px: '$10' }}>
        <Button variant="flat">Flat</Button>
        <Button variant="flat" disabled>
          Flat (disabled)
        </Button>
      </Flex>
      <Flex gap="sm" css={{ px: '$10' }}>
        <Button variant="link" as="a" href="#">
          Link
        </Button>
        <Button variant="link" as="a" href="#" disabled>
          Link (disabled)
        </Button>
      </Flex>
    </Flex>
  </ThemeProvider>
</Playground>

## With elements

<Playground>
  <Flex flow="column" cross="start" gap="xl" css={{ p: '$8', bc: '$gray50' }}>
    <Button variant="primary" gap="2">
      <Icon as={DotsVerticalIcon} />
      <span>Wrapped</span>
    </Button>
    <Button variant="primary" gap="2">
      <Icon as={DotsVerticalIcon} />
      Not wrapped
    </Button>
    <Button variant="outline">
      <Icon as={DotsVerticalIcon} variant="secondary" />
    </Button>
  </Flex>
</Playground>
