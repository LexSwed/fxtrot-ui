---
name: Button
---

import { Playground } from 'dokz';
import { HiDotsVertical } from 'react-icons/hi';
import { Box, Flex, Button, ThemeProvider, Icon } from '@fxtrot/ui';

## Variant

<Playground>
  <Flex space="xl" css={{ py: '$8', bc: '$gray050' }}>
    <Flex flow="row" space="sm" css={{ px: '$10' }}>
      <Button>Primary</Button>
      <Button disabled>Primary (disabled)</Button>
    </Flex>
    <Flex flow="row" space="sm" css={{ px: '$10' }}>
      <Button variant="outline">Outline</Button>
      <Button variant="outline" disabled>
        Outline (disabled)
      </Button>
    </Flex>
    <Flex flow="row" space="sm" css={{ px: '$10' }}>
      <Button variant="secondary">Secondary</Button>
      <Button variant="secondary" disabled>
        Secondary (disabled)
      </Button>
    </Flex>
    <Flex flow="row" space="sm" css={{ px: '$10' }}>
      <Button variant="flat">Flat</Button>
      <Button variant="flat" disabled>
        Flat (disabled)
      </Button>
    </Flex>
    <Flex flow="row" space="sm" css={{ px: '$10' }}>
      <Button variant="link" as="a" href="#">
        Link
      </Button>
      <Button variant="link" as="a" href="#" disabled>
        Link (disabled)
      </Button>
    </Flex>
  </Flex>
</Playground>

## Theming

Use `ThemeProvider` component to change the primary color of the button.

<Playground>
  <ThemeProvider theme="black">
    <Flex space="xl" css={{ py: '$8', bc: '$surfaceStill' }}>
      <Flex flow="row" space="sm" css={{ px: '$10' }}>
        <Button>Primary</Button>
        <Button disabled>Primary (disabled)</Button>
      </Flex>
      <Flex flow="row" space="sm" css={{ px: '$10' }}>
        <Button variant="secondary">Secondary</Button>
        <Button variant="secondary" disabled>
          Secondary (disabled)
        </Button>
      </Flex>
      <Flex flow="row" space="sm" css={{ px: '$10' }}>
        <Button variant="flat">Flat</Button>
        <Button variant="flat" disabled>
          Flat (disabled)
        </Button>
      </Flex>
      <Flex flow="row" space="sm" css={{ px: '$10' }}>
        <Button variant="link" as="a" href="#">
          Link
        </Button>
        <Button variant="link" as="a" href="#" disabled>
          Link (disabled)
        </Button>
      </Flex>
    </Flex>
  </ThemeProvider>
</Playground>

## With elements

<Playground>
  <Flex space="xl" css={{ p: '$8', bc: '$gray050' }}>
    <Button>
      <Icon as={HiDotsVertical} />
      <span>Wrapped</span>
    </Button>
    <Button>
      <Icon as={HiDotsVertical} />
      Not wrapped
    </Button>
    <Button variant="outline">
      <Icon as={HiDotsVertical} variant="secondary" />
    </Button>
  </Flex>
</Playground>
