---
name: Spinner
---

import { Flex, Spinner } from '@fxtrot/ui';

# Spinner

<Playground>
  <Flex gap="lg" cross="center" css={{ bc: '$surface', p: '$8' }}>
    <Spinner size="sm" />
    <Spinner size="md" />
    <Spinner size="lg" />
  </Flex>
</Playground>

export default (props) => <MainLayout {...props} meta={meta} />;
