---
name: Switch
---

import { Playground } from 'dokz';
import { ThemeProvider, Flex, Switch } from '@fxtrot/ui';

## Switch

<Playground>
  <Flex space="xl" flow="row" css={{ p: '$8', bc: '$surfaceStill' }}>
    <Switch label="Default" />
    <Switch label="Disabled" disabled />
    <Switch label="Checked disabled" disabled defaultChecked />
  </Flex>
</Playground>

## Secondary text

<Playground>
  <Flex space="xl" css={{ p: '$8', bc: '$surfaceStill' }}>
    <Switch label="Format on save" secondaryLabel="(required)" required />
  </Flex>
</Playground>

## Theming

Use `ThemeProvider` component to change the primary color of the button.

<Playground>
  <Flex space="xl" css={{ p: '$8', bc: '$surfaceStill' }}>
    <Flex flow="row" space="md">
      <ThemeProvider theme="rose">
        <Switch label="Format on save" flow="row-reverse" space="md" />
        <Switch label="Format on save (disabled)" disabled flow="row-reverse" space="md" />
      </ThemeProvider>
    </Flex>
    <Flex flow="row" space="md">
      <ThemeProvider theme="green">
        <Switch label="Format on save" />
        <Switch label="Format on save (disabled)" disabled />
      </ThemeProvider>
    </Flex>
  </Flex>
</Playground>
