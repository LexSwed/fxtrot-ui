---
title: Fxtrot UI
---

export default (props) => <Layout meta={meta} {...props} />;

# Install

Install component library in your React project

```bash
yarn add @fxtrot/ui
```

Use components

```jsx
import { Box } from '@fxtrot/ui';

<Box p={2}>Content</Box>;
```
