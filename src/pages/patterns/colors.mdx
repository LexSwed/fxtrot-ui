---
name: Palette
---

import { Box } from '@fxtrot/ui';
import * as palette from 'src/lib/theme/palette';

# Palette

export const styles = {
  wrapper: {
    display: 'grid',
    gridAutoFlow: 'row',
    gridTemplateColumns: 'repeat(auto-fill, 220px)',
    gap: '$2',
  },
  swatch: {
    display: 'flex',
    gap: '$3',
    alignItems: 'center',
  },
  text: {
    textSize: '$sm',
    fontWeight: '600',
  },
  text2: {
    textSize: '$sm',
    fontFamily: '$mono',
    color: '$gray700',
  },
  colorItem: {
    flexShrink: 0,
    size: '50px',
    borderRadius: '$round',
    transition: '0.2s',
    cursor: 'pointer',
    boxShadow: '$sm',
  },
};

<Box>
  {Object.entries(palette).map(([title, colors]) => (
    <React.Fragment key={title}>
      <h2>{title}</h2>
      <Box css={styles.wrapper}>
        {Object.entries(colors).map(([name, color]) => (
          <Box css={styles.swatch} key={name}>
            <Box css={{ ...styles.colorItem, bc: name }} />
            <div>
              <Box css={styles.text}>{name}</Box>
              <Box css={styles.text2}>{color}</Box>
            </div>
          </Box>
        ))}
      </Box>
    </React.Fragment>
  ))}
</Box>
