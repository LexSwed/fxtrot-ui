---
name: Palette
---

import { Box, Flex, Grid } from '@fxtrot/ui';
import * as colors from 'src/lib/theme/colors';

# Palette

export const styles = {
  text: {
    textSize: '$sm',
    fontWeight: '600',
  },
  text2: {
    textSize: '$sm',
    fontFamily: '$mono',
    color: '$gray700',
  },
  colorItem: {
    'flexShrink': 0,
    'width': '100%',
    'height': '50px',
    'transition': '0.24s',
    'cursor': 'pointer',
    'boxShadow': '$xl',
    'br': '$md',
    '&:hover': {
      boxShadow: '$xs',
    },
  },
};

<Box>
  {Object.entries(colors).map(([title, colors]) => (
    <React.Fragment key={title}>
      <h2>{title}</h2>
      <Flex gap="4" wrap="nowrap" css={{ overflow: 'auto' }}>
        {Object.entries(colors).map(([name, color]) => (
          <Flex flow="column" cross="start" gap="3" key={name}>
            <Box
              css={styles.colorItem}
              bc={`$${name}`}
              boxShadow={`0 4px 6px -1px $colors$${name}, 0 2px 4px -1px rgba(0, 0, 0, 0.06)`}
            />
            <div>
              <Box css={styles.text}>{name}</Box>
              <Box css={styles.text2}>{color}</Box>
            </div>
          </Flex>
        ))}
      </Flex>
    </React.Fragment>
  ))}
</Box>
